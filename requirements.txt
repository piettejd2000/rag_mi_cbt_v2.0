# Core dependencies
  streamlit==1.29.0
  anthropic>=0.109.0
  chromadb>=1.5.9
  sentence-transformers>=2.2.0
  plotly>=6.8.0

  # Protobuf - compatible version
  protobuf==3.20.3

  # PyTorch dependencies (CPU only for cloud deployment)
  --find-links https://download.pytorch.org/whl/cpu
  torch==2.0.1+cpu
  torchvision==0.15.2+cpu

  # Additional dependencies
  numpy<2.0.0
  pandas>=1.5.0
  python-dotenv>=1.0.0
  tenacity>=9.0.0
